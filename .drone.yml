---
kind: pipeline
type: docker
name: ml_all master

platform:
  arch: amd64
  os: linux

steps:
- name: Build latest ml_all docker image
  image: plugins/docker
  settings:
    dockerfile: Dockerfile
    context: .
    registry: quay.io
    repo: quay.io/logivations/l4t-ros-tf
    privileged: true
    tags:
      - test_latest
      - ${DRONE_COMMIT_SHA}
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
  when:
    branch: main

trigger:
  event:
    include:
    - push

---
kind: pipeline
type: docker
name: ml_all custome branch

platform:
  arch: amd64
  os: linux

steps:
- name: Build custom ml_all_image
  image: plugins/docker
  settings:
    dockerfile: Dockerfile
    context: .
    registry: quay.io
    repo: quay.io/logivations/l4t-ros-tf
    privileged: true
    tags:
      - ${DRONE_PULL_REQUEST}
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password

trigger:
  event:
    include:
    - pull_request
